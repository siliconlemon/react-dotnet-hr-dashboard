/** Minimum time the loading overlay stays visible so the spinner can complete a full cycle. */
export const VIEW_MIN_LOADING_MS = 1000;

/** Fade-in duration for main view content after loading finishes. */
export const VIEW_CONTENT_FADE_MS = 380;

/** Fade-in / fade-out for the loading overlay (spinner scrim). */
export const VIEW_LOADER_FADE_MS = 240;
