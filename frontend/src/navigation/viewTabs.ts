/** URL-backed tabs for `/employees` (directory is default route with no segment). */
export type EmployeesViewTab = 'directory' | 'onboard' | 'edit' | 'remove';

/** URL-backed tabs for `/leave`. */
export type LeaveManagementViewTab = 'ledger' | 'lookup';
